#!/bin/sh

if [ ! -f "/home/poem/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/poem/.config/chezmoi"
    chezmoi age decrypt --output "/home/poem/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/poem/.config/chezmoi/key.txt"
fi
